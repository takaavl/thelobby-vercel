[project]
name = "the-lobby"
version = "1.0.0"
description = "Private Valorant inhouse stats hub"
requires-python = ">=3.13,<3.14"
dependencies = [
  "fastapi>=0.116,<1",
  "uvicorn>=0.35,<1",
  "libsql>=0.1.11,<1",
]

[tool.vercel]
entrypoint = "server:app"
